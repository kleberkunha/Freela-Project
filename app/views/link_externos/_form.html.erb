<%= form_with(model: link_externo) do |form| %>
  <% if link_externo.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(link_externo.errors.count, "error") %> prohibited this link_externo from being saved:</h2>

      <ul>
        <% link_externo.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :title, style: "display: block" %>
    <%= form.text_field :title %>
  </div>

  <div>
    <%= form.label :url_page, style: "display: block" %>
    <%= form.text_field :url_page %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
