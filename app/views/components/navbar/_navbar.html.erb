<nav class="navbar navbar-expand-lg navbar-light bg-light control-navbar">
  <a class="navbar-brand" href="#"></a>
  <button
    class="navbar-toggler"
    type="button"
    data-toggle="collapse"
    data-target="#navbarNav"
    aria-controls="navbarNav"
    aria-expanded="false"
    aria-label="Toggle navigation"
  >
    <span class="navbar-toggler-icon"></span>
  </button>
  <div class="collapse navbar-collapse" id="navbarNav">
    <ul class="navbar-nav">
      <li class="nav-item">
        <%= link_to "Accueil", root_path, class: "nav-link" %>
      </li>
      <div class="dropdown nav-item">
        <button
          class="btn button-links dropdown-toggle"
          type="button"
          id="dropdownMenuButton"
          data-toggle="dropdown"
          aria-haspopup="true"
          aria-expanded="false"
        >
          Links externos
        </button>
        <div class="dropdown-menu" aria-labelledby="dropdownMenuButton">
          <% if !@link_externos.nil? %>
            <% @link_externos.each do |link| %>
              <a class="dropdown-item" href="<%= link.url_page %>" target="_blank"
                ><%= link.title %></a
              >
            <% end %>
          <% else %>
            <p>Sem links no momento</p>
          <% end %>
        </div>
      </div>
      <li class="nav-item">
        <%= link_to 'Blog', blog_index_path, class:"nav-link"%>
      </li>
      <li class="nav-item">
        <%= link_to 'Contact', contact_index_path, class:"nav-link"%>
      </li>
      <% if !user_signed_in? %>
        <li class=""><%= link_to '.', new_user_session_path%></li>
      <% else %>
        <li class="nav-item">
          <%= link_to "Log out", destroy_user_session_path, :method => :delete ,
          class: "nav-link" %>
        </li>
        <li class="nav-item">
          <%= link_to 'ADM', user_index_path, class:"nav-link" %>
        </li>
      <% end %>
    </ul>
  </div>
</nav>
